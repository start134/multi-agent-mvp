# Optional. If not set, the app runs in mock mode with deterministic outputs.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# SQLite database path
DATABASE_URL=sqlite:///./data/agent_mvp.db

# App settings
APP_NAME=Multi Agent Automation MVP
